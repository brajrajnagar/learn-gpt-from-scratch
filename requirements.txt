# Requirements for Learn GPT From Scratch
# Install with: pip install -r requirements.txt

# Core dependency - numerical computing
numpy>=1.20.0

# Optional: For PyTorch version (future extension)
# torch>=2.0.0

# Optional: For progress bars during training
# tqdm>=4.60.0